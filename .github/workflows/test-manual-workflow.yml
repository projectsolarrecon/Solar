name: Test Manual Workflow

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm workflow is running
        run: echo "SOLAR manual workflow test is working"